# Audio download
yt-dlp>=2024.1.0

# Bundled ffmpeg binary, used automatically if ffmpeg isn't on your PATH.
imageio-ffmpeg>=0.4.9

# Bass source separation (pulls in torch). Optional at runtime via --no-separate.
demucs>=4.0.0

# Pitch detection / transcription
librosa>=0.10.0
numpy>=1.23
soundfile>=0.12.1
